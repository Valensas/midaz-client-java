# Midaz Ledger Java Client

Midaz Java client, generated by OpenAPI generator.

## Setup
In the project directory, run the following command to generate and publish the API.

```shell
./gradlew clean
./gradlew generateAndPublishApi
```